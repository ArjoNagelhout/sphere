#ifndef SPHERE_MESH_H
#define SPHERE_MESH_H

namespace engine {

    class Mesh{

    public:
        explicit Mesh();
        ~Mesh();

    private:
        // should contain a buffer with data for vertices and indices
    };
}

#endif //SPHERE_MESH_H
